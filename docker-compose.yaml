version: "3"
services:
  firesquadxx_front:
    container_name: firesquadxx_front_container
    build:
      context: ./backend
      dockerfile: Dockerfile
    volumes:
      - ./backend:/app
      - /backend/node_modules
    command: npm run dev
    ports:
      - 8000:8000
    restart: always
